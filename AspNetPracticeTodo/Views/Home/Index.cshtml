@model IEnumerable<TodoList>
@{
    ViewData["Title"] = "Home";
}

<h1>Practice To-do</h1>

@if (Model.Any())
{
    <ol>
        @foreach (var list in Model)
        {
            <li>@list.Name</li>
        }
    </ol>
}
else
{
    <i>No lists</i>
}
